/* ================================================================
   ZEUS SENSI — VALID PINS FILE
   ================================================================
   👉 ADD YOUR PINS HERE. This file is plain text — no coding needed.
   - Add as MANY pins as you want (more than 1 works).
   - Pins can contain LETTERS and NUMBERS only (A-Z, 0-9).
   - No spaces, no symbols (no @ # $ % etc.).
   - Pins are checked case-insensitively, so "zeus2026" also works.
   - Each pin goes on its own line, inside the [ ] brackets,
     wrapped in quotes, with a comma at the end.

   EXAMPLE — add a new pin:
     "MYNEWPIN123",
   ================================================================ */

window.VALID_PINS = [
  "ZEUS2026",
  "DANNY777",
  "XYLO123",
  "PREMIUMVIP",
  "SENSI100",
  "FFPRO777",
  "HEADSHOT01",
  "MRDANNY1"
];

/* ================================================================
   OPTIONAL CONFIG
   - protectHudSection: true  = the "CREATORS CUSTOM HUD" section is
                                hidden until a valid PIN is entered.
     protectHudSection: false = HUD section is public (like ffsensi.com).
   Change true/false below if you want.
   ================================================================ */
window.ZEUS_CONFIG = {
  protectHudSection: false
};
